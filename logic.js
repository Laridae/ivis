alert("Du har virus!")

var tick = 1

function show_next_question(d) {
 var currentQ = document.getElementById("question" + d);
 var nextQ = document.getElementById("fråga" + (d+1));
 
 hide div.d; 
 show div.x;
}
